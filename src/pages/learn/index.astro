---
import BaseLayout from "@/layouts/BaseLayout.astro";
import LearnTabs from "@/components/LearnTabs.astro";
import PageHero from "@/components/PageHero.astro";
import { learnIntro } from "@/content/learn";
import { localizePath } from "@/lib/i18n";

const locale = "en";
---

<BaseLayout active="learn" description={learnIntro.summary.en} locale={locale} path="/learn/" title="Learn Hub for Hermes Agent">
  <PageHero
    className="page-hero learn-hero"
    ctas={[
      { href: localizePath("/#quick-start", locale), label: learnIntro.ctas[0].label.en, variant: learnIntro.ctas[0].variant },
      { href: localizePath(learnIntro.ctas[1].href, locale), label: learnIntro.ctas[1].label.en, variant: learnIntro.ctas[1].variant },
    ]}
    kicker={learnIntro.kicker}
    summary={learnIntro.summary.en}
    title={learnIntro.title.en}
  />
  <LearnTabs locale={locale} />
</BaseLayout>
